import Mathlib.NumberTheory.FLT.Basic

/-!
Challenge statement for Comparator: Fermat's Last Theorem for exponent 107 --
the first regular prime above 100, beyond the range of the concurrent
Kummer-criterion formalization. The transitive closure of imports here is the
trusted side: only Mathlib's own `FermatLastTheoremFor` is required.
-/

theorem flt_107 : FermatLastTheoremFor 107 := sorry
